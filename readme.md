To run, install node modules `npm install`, use android studio to create a device running SDK 31, and build.

If CMAKE error is given, run in `android` dir `./gradlew clean && ./gradlew assembleDebug`
